.subnav
    h1 Your Lists
article
  .infiniteCarousel
    .wrapper
      ul
        - each shelf in json.shelves.user_shelf
          li.shelf(id='#{shelf.name}')
            h4
              a(href='/goodreads/list/#{shelf.name}') #{shelf.name} 
              #{shelf.book_count['#']}
            .shelfItems(id='#{shelf.name}')